# my guix config

tried to configure sway like on my previous nixos setup, got fed up with issues, switched to kde. sway config is still at sway.scm and can be enabled using instructions there.

TODO: make as much as possible of actual package configuration declarative (mostly stuff that's in ~/.config).

